<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Dashboard</title>
  </head>
  <body>
    <h1>Admin Dashboard</h1>
    <p>Velkommen til administratordashbordet!</p>

    <h2>Dine blogginnlegg:</h2>
    <% if (posts && posts.length > 0) { %>
    <ul>
      <% posts.forEach(post => { %>
      <li><%= post.title %></li>
      <% }); %>
    </ul>
    <% } else { %>
    <p>Du har ingen blogginnlegg ennå.</p>
    <% } %>

    <a href="/add-post">Legg til nytt blogginnlegg</a>
  </body>
</html>
